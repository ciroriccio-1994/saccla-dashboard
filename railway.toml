[build]
builder = "nixpacks"

[deploy]
startCommand = "playwright install chromium && playwright install-deps chromium && streamlit run app.py --server.port=$PORT --server.address=0.0.0.0"
restartPolicyType = "on_failure"
